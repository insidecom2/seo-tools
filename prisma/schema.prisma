generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native", "debian-openssl-1.1.x"]
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model tb_price {
  f_id       Int       @id @default(autoincrement())
  f_symbol   String?   @db.VarChar(100)
  f_price    Float?    @db.Float
  f_datetime DateTime? @db.Timestamp(0)
}
